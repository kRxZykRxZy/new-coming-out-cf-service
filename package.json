{
  "name": "new-coming-out-cf-service",
  "version": "1.0.0",
  "description": "A service with PostgreSQL and SQLite database support",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "pnpm node index.js",
    "migrate": "pnpm npx sequelize-cli db:migrate",
    "migrate:undo": "pnpm npx sequelize-cli db:migrate:undo"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.11.0",
    "sequelize": "^6.35.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}